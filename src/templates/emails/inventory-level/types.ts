
export type InventoryLevelTemplateDataType = {
  inventory_level: {
    id: string;
    location_id: string;
    stocked_quantity: string;
    reserved_quantity: string;
    available_quantity: string;
  }
};
